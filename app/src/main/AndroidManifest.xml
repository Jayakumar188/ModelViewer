<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.ModelViewer"
        android:hardwareAccelerated="true"
        android:largeHeap="true">
        <!-- largeHeap: five decoded glTF assets (textures + vertex data)
             can briefly coexist in Java/native heap while a new model is
             being added; this gives low-RAM devices a bit more headroom
             instead of hitting an early OOM during the add-model flow. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
